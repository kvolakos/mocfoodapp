<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.moc2go">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HappyPlaceMenu"></activity>
        <activity android:name=".GrillMasterMenu" />
        <activity android:name=".TutusCyberCafeMenu" />
        <activity android:name=".UnderCafMenu" />
        <activity android:name=".BuckStopMenu" />
        <activity android:name=".Home" />
        <activity android:name=".Settings" />
        <activity android:name=".Orders" />
        <activity android:name=".Restaurants" />
        <activity android:name=".OrderHistory" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>